import { LoginFormTemplate } from './login-form-template';

describe('LoginFormTemplate', () => {
  it('should create an instance', () => {
    expect(new LoginFormTemplate()).toBeTruthy();
  });
});
