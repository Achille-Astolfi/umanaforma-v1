import { TokenRequestSubscription } from './token-request-subscription';

describe('TokenRequestSubscription', () => {
  it('should create an instance', () => {
    expect(new TokenRequestSubscription()).toBeTruthy();
  });
});
