import { TokenRequestIscrizione } from './token-request-iscrizione';

describe('TokenRequestIscrizione', () => {
  it('should create an instance', () => {
    expect(new TokenRequestIscrizione()).toBeTruthy();
  });
});
